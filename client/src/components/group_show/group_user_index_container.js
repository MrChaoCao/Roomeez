import { connect } from 'react-redux';
import GroupUserIndex from './group_user_index';
import { withRouter } from 'react-router';

export default withRouter(connect(
  null,
  null
)(GroupUserIndex));
